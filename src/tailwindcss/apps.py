from django.apps import AppConfig


class TailwindcssConfig(AppConfig):
    """
    Django app configuration for Tailwind CSS integration.

    Provides Tailwind CSS styling framework integration for the Lodestar project,
    enabling utility-first CSS approach with custom space-themed design system.
    """

    name = "tailwindcss"
